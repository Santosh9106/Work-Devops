

Welcome the Devops
get some new git hub commands

mkdir
ls
cd..
cd  
git config

rm -r <file>

git remote add origin "link"

git pull origin master 

git status

git add file name

git commit -m "message"

git add -A   <if you want to add more than 3 files at a atime>

git commit -a -m "message" <if you want to commmit all give small -a>

git log   <to check the status > 

git  checkout master  <to go back to master from firstbranch>

git log   

git branch firstbranch

git checkout firstbranch    <checkout from master to firstbranch>

git merge firstbranch   <be in master and merge the firstbranch in it>

cat Devops4.txt           <what is inside the txt file written.>

git checkout master <first create few files in branch and add it and commit>

git rebase firstbranch  <it will save automatically and show the new files>

git checkout master

git rebase firstbranch   <it will show the newly added files of branch in master without merging>


